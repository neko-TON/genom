# Lux Pack Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Professional-project polish: OG/Twitter share previews with a branded og.jpg, a branded 404 page, and button/card micro-physics.

**Architecture:** Task 0 (coordinator, done before dispatch): generate `og.jpg` via browser canvas into the worktree root. Task 1 (implementer): head metas on both pages, `404.html`, CSS micro-states; commits og.jpg alongside. Task 2 (coordinator): browser pass, review, merge, deploy, prod checks.

**Tech Stack:** Static HTML/CSS; no JS changes. Verification: 58-test suite, greps, browser, prod curl.

**Spec:** `docs/superpowers/specs/2026-08-30-lux-pack-design.md`.

## Global Constraints

- No page copy changes; only head additions, the new 404.html, og.jpg, and CSS state rules.
- Only index.html / console.html / front.css / app.css change + new files og.jpg, 404.html.
- Commit messages end with:
  `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`

---

### Task 1: Metas + 404 + micro-physics

**Files:**
- Modify: `index.html`, `console.html`, `front.css`, `app.css`
- Create: `404.html`
- Include in commit: `og.jpg` (уже лежит в корне ворктри — сгенерирован координатором; НЕ пересоздавать)

- [ ] **Step 1: index.html head** — immediately AFTER the existing `<meta name="description" …>` line insert:

```html
<meta name="theme-color" content="#020804">
<meta property="og:type" content="website">
<meta property="og:url" content="https://genom-iota.vercel.app/">
<meta property="og:title" content="Genom Agent — $GENOM">
<meta property="og:description" content="An index token where a 3% tax on trades keeps purchasing tokenized stocks for a common basket. Weights are chosen by an autonomous agent, the mandate is policed by a contract, and each decision is committed as a hash before it runs.">
<meta property="og:image" content="https://genom-iota.vercel.app/og.jpg">
<meta property="og:image:width" content="1200">
<meta property="og:image:height" content="630">
<meta name="twitter:card" content="summary_large_image">
<meta name="twitter:title" content="Genom Agent — $GENOM">
<meta name="twitter:description" content="A self-driving index token with hard limits written into its DNA. 3% tax → holders' basket. Robinhood Chain.">
<meta name="twitter:image" content="https://genom-iota.vercel.app/og.jpg">
```

- [ ] **Step 2: console.html head** — immediately AFTER the `<title>` line insert:

```html
<meta name="description" content="Live observer console of the Genom node: basket, NAV curve, sealed decisions and the mandate guard.">
<meta name="theme-color" content="#020804">
<meta property="og:type" content="website">
<meta property="og:url" content="https://genom-iota.vercel.app/console.html">
<meta property="og:title" content="Genom Console — $GENOM">
<meta property="og:description" content="Live observer console of the Genom node: basket, NAV curve, sealed decisions and the mandate guard.">
<meta property="og:image" content="https://genom-iota.vercel.app/og.jpg">
<meta property="og:image:width" content="1200">
<meta property="og:image:height" content="630">
<meta name="twitter:card" content="summary_large_image">
<meta name="twitter:title" content="Genom Console — $GENOM">
<meta name="twitter:description" content="Live observer console of the Genom node: basket, NAV curve, sealed decisions and the mandate guard.">
<meta name="twitter:image" content="https://genom-iota.vercel.app/og.jpg">
```

- [ ] **Step 3: Create `404.html`:**

```html
<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>404 — Genom</title>
<meta name="theme-color" content="#020804">
<link rel="icon" type="image/svg+xml" href="/favicon.svg">
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Unbounded:wght@600&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet">
<style>
  * { box-sizing: border-box; margin: 0; padding: 0; }
  body {
    min-height: 100vh; display: flex; align-items: center; justify-content: center;
    background: #020804 radial-gradient(ellipse 120% 95% at 45% 40%, transparent 40%, rgba(2, 8, 4, .9) 100%);
    color: #eafcef; font: 14px/1.6 "JetBrains Mono", monospace; text-align: center;
  }
  .box { padding: 40px 24px; }
  .code {
    font-family: Unbounded, sans-serif; font-weight: 600; font-size: clamp(90px, 18vw, 160px);
    line-height: 1; color: transparent; -webkit-text-stroke: 2px rgba(99, 245, 166, .5);
  }
  .gene { margin-top: 18px; font-size: 14px; letter-spacing: .35em; color: #63f5a6; }
  .caret { display: inline-block; width: 10px; height: 3px; background: #63f5a6; margin-left: 8px; vertical-align: baseline; }
  @media (prefers-reduced-motion: no-preference) { .caret { animation: blink 1.1s steps(1) infinite; } }
  @keyframes blink { 50% { opacity: 0; } }
  .note { margin-top: 14px; font-size: 12px; letter-spacing: .06em; color: #637e6f; }
  .back {
    display: inline-block; margin-top: 34px; padding: 14px 24px;
    font-size: 11px; letter-spacing: .14em; color: #63f5a6; text-decoration: none;
    border: 1px solid rgba(99, 245, 166, .26); background: rgba(2, 8, 4, .4);
    transition: background .2s, color .2s, transform .1s;
  }
  .back:hover { background: #63f5a6; color: #04140b; }
  .back:active { transform: translateY(1px); }
</style>
</head>
<body>
<div class="box">
  <p class="code">404</p>
  <p class="gene">GENE NOT FOUND<span class="caret"></span></p>
  <p class="note">This sequence does not exist in the genome.</p>
  <a class="back" href="/">← BACK TO THE GENOME</a>
</div>
</body>
</html>
```

- [ ] **Step 4: front.css micro-physics**
  - In the `.btn` rule replace `transition: background .2s, color .2s, border-color .2s;` with `transition: background .2s, color .2s, border-color .2s, transform .1s;` and add a new rule right after `.btn:hover { … }`:

```css
.btn:active { transform: translateY(1px); }
```

  - In the `#caCopy` rule replace `transition: background .2s, color .2s;` with `transition: background .2s, color .2s, transform .1s;` and add after `#caCopy:hover { … }`:

```css
#caCopy:active { transform: translateY(1px); }
```

  - Append at EOF:

```css
/* ---------- card micro-states ---------- */
.gene, .mcard { transition: background .25s, border-color .25s; }
.gene:hover, .mcard:hover { background: rgba(99, 245, 166, .06); border-color: rgba(99, 245, 166, .4); }
```

- [ ] **Step 5: app.css micro-physics** — in the `.ctl` rule replace `transition: background .2s, color .2s;` with `transition: background .2s, color .2s, transform .1s;` and add after `.ctl:hover { … }`:

```css
.ctl:active { transform: translateY(1px); }
```

- [ ] **Step 6: Verify + commit**

Run: `python3 -m unittest discover -s tests 2>&1 | tail -3` — 58 OK.
Sanity: `grep -c "og:image" index.html console.html` → 2 и 2 (property+twitter… точнее og:image+og:image:width/height и twitter:image содержат подстроку — фактически 4 и 4; отчитайся фактами); `ls -la og.jpg 404.html` — оба существуют, og.jpg > 20KB; `grep -c ":active" front.css app.css` → 2 и 1.

```bash
git add index.html console.html 404.html og.jpg front.css app.css
git commit -m "feat: пакет «Люкс» — OG-превью с брендовой картинкой, страница 404, микрофизика кнопок и карточек

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 2: Coordinator — browser pass, review, merge, deploy

- [ ] Открыть 404.html локально (файлом или через сервер ворктри /404.html — server.py отдаёт .html статику): 404 / GENE NOT FOUND / каретка / кнопка домой.
- [ ] Кнопки: клик «вдавливает» (.btn, #caCopy, .ctl); карточки оживают ховером.
- [ ] og.jpg открывается и выглядит по спеке.
- [ ] Review; merge per user's menu; push; deploy. Prod: `curl -I /og.jpg` → 200 image/jpeg; `curl /nesuschestvuet` → 404-страница (grep GENE NOT FOUND); метатеги в HTML прода.
